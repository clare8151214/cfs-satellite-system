# 研究進度逐字稿

## 第 1 頁：研究進度

各位老師好，今天要報告的是目前 cFS 衛星系統移植到 FreeRTOS 的研究進度。

我們原本的衛星端是以 Ubuntu ARM64 虛擬機作為執行環境，現在的研究方向是把這個衛星端概念改成 FreeRTOS，並且保留 cFS/cFE 的核心架構。

目前我們已經做到一個可展示的 POC：cFE 可以在 QEMU 模擬的 ARM Cortex-M3 上啟動，mission app 可以被 cFE 載入並執行，同時也能把衛星任務遙測送回 cFS-GroundSystem 地面站。

這份簡報會依序說明研究目標、目前架構、已完成項目、展示方式，以及目前限制與下一步。

## 第 2 頁：研究目標

這一頁主要說明研究目標。

原本的衛星系統是在 Ubuntu ARM64 VM 上執行。這樣的環境比較接近一般 Linux 系統，可以使用 Linux 的 socket、filesystem 等功能。

但是老師希望我們往 FreeRTOS 的方向研究，也就是讓衛星端更接近即時作業系統的環境。這件事不是單純把程式複製過去，因為 FreeRTOS 和 Ubuntu 在系統資源、檔案系統、網路支援、動態載入能力上都有差異。

所以這次 POC 的目標分成三個層次。第一，確認 cFE 可以在 FreeRTOS target 上開機。第二，讓 cFE 真的啟動一個 mission app。第三，讓這個 mission app 的資料可以回到 cFS-GroundSystem，形成一個可以展示的衛星端到地面站資料流。

目前這三個層次都已經有初步成果。

## 第 3 頁：目前架構

這一頁是目前系統架構。

右邊是衛星端，也就是 QEMU 模擬的 ARM Cortex-M3，machine 使用的是 mps2-an385。裡面執行的是 FreeRTOS image，產出的核心檔案是 core-mps2。

FreeRTOS image 裡面包含 cFE、OSAL、PSP，以及我們新增的 SAT_SAMPLE_APP。這個 app 現在不是單純 heartbeat，而是會輸出 Satellite Mission HK，也就是任務狀態遙測，例如模式、狀態、執行時間、payload sample 數量，以及 battery percent。

中間是 satellite-ground-bridge.py。因為目前這個 FreeRTOS port 的 OSAL socket 還沒有完成，所以我們先用 host-side bridge 做 POC 串接。bridge 會讀取 QEMU console 裡的 SAT_MISSION_HK 輸出，轉成 cFS-GroundSystem 可以接收的 UDP telemetry packet。

左邊是地面站，也就是 cFS-GroundSystem。RoutingService.py 會接收 UDP 2234 的 telemetry，再透過 ZeroMQ 發給 Telemetry GUI。Command GUI 則會送 UDP 1234 的 command 到 bridge。

所以目前的資料流是：mission app 產生遙測，bridge 轉成 UDP 2234 給地面站；地面站送 command 到 UDP 1234，bridge 接收後回傳 command ACK telemetry。

## 第 4 頁：已完成項目

這一頁列出目前已完成的項目。

第一，FreeRTOS cFE boot 已經完成。cFE 可以在 QEMU Cortex-M3 上啟動，而且可以進入 OPERATIONAL state。

第二，我們補上了 /cf/cfe_es_startup.scr。因為 FreeRTOS 這個 port 的 filesystem 還不完整，所以我們用 embedded startup script 的方式，讓 cFE 啟動時可以找到 startup script。

第三，mission app 已經可以被 cFE 啟動。原本只是 heartbeat sample，現在改成 Satellite Mission HK app，會模擬 SAFE mode 進入 NOMINAL mode，並持續輸出 mission telemetry。

第四，我們補強了 FreeRTOS loader stub。cFE 啟動 static app 時需要 module load 和 symbol lookup，但這個 FreeRTOS port 原本沒有完整 dynamic loader，所以我們讓它可以查 static module 和 static symbol。

第五，加入了 Ground bridge。bridge 會把 QEMU console 的 mission telemetry 轉成 UDP telemetry，並監聽 GroundSystem 的 command port。

第六，GroundSystem 端也有新增頁面。Telemetry System 裡有 Satellite Mission HK，Command System 裡也有 Satellite Mission command entry。

## 第 5 頁：展示流程與驗證

這一頁是展示流程和驗證方式。

展示時需要開兩個 terminal。

第一個 terminal 啟動地面站，指令是 cd 到 cFS-GroundSystem，然後執行 python3 GroundSystem.py。這會開啟 cFS-GroundSystem 主畫面，同時 RoutingService 會開始等待 UDP telemetry。

第二個 terminal 啟動 FreeRTOS 衛星 POC，指令是 cd 到 cfs-freertos-satellite，然後執行 ./start-satellite-freertos-poc.sh。這個腳本預設會透過 satellite-ground-bridge.py 啟動 QEMU。

啟動後，可以在 log 裡看到 SAT_MISSION_HK。bridge 會把它轉成 UDP 2234 的 telemetry。地面站的 Telemetry System 裡可以打開 Satellite Mission HK 頁面，看到 command counter、error counter、mission mode、mission status、uptime、payload samples 和 battery percent。

如果從 GroundSystem 的 Command System 發送 Satellite Mission 的 Mission No-Op，bridge 會收到 packet id 0x1882，command code 0，然後回傳 command accepted 的 telemetry。這時 telemetry 裡的 command counter 會增加，表示 command path 在 POC 層級已經接通。

所以目前展示重點是：FreeRTOS 衛星端會產生任務遙測，地面站可以接收；地面站送出的命令也能被 bridge 接收並回傳 ACK。

## 第 6 頁：限制與下一步

最後這一頁說明目前限制和下一步。

目前最大的限制是，這還不是完整的 native flight command path。也就是說，command packet 目前是由 host-side bridge 接收並回 ACK，還沒有真正注入到 FreeRTOS image 裡的 cFE Software Bus。

原因是目前這個 FreeRTOS OSAL socket layer 還是 stub，OS_SocketSendTo 和 OS_SocketRecvFrom 這類功能尚未實作。因此還不能直接在 FreeRTOS flight image 裡跑像 Ubuntu cFS 那樣的 CI_LAB 和 TO_LAB socket path。

下一步有兩個可能方向。第一個方向是實作 FreeRTOS target 的 OSAL socket，讓 CI_LAB 和 TO_LAB 可以 native 運作。第二個方向是做 UART RX command ingest，也就是透過 QEMU UART 把地面站 command 傳進 FreeRTOS，再轉進 cFE Software Bus。

之後也可以把目前的 host bridge telemetry path 改成 native TO_LAB-style UDP telemetry，並擴充更多 mission command 和 packet definition。

總結來說，目前已經從「cFE 能不能在 FreeRTOS boot」推進到「mission app 能跑、能產生任務遙測、能接回 cFS-GroundSystem 展示」。下一階段的重點會是把 command/telemetry 從 POC bridge 推進到 FreeRTOS flight image 的 native 路徑。
